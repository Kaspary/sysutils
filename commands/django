########################
### Utils for django ###
########################

function django(){
    if [[ $@ == "run" ]]; then
        echo 'run project...'
        source env/bin/activate && python manage.py runserver
    fi
}

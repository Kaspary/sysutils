#########################
### Utils for pyramid ###
#########################

function pyramid(){
	if [[ $@ == "run" ]]; then
         echo 'run project...'
         source venv/bin/activate && pserve --reload development.ini
    fi
}
